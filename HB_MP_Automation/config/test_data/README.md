# Test fixtures

| Path | Purpose |
|---|---|
| `*.json` | Feature payloads loaded by `test_data_reader` / `test_data()` |
| `uploads/` | Static files attached in UI tests (`test_upload.txt`) |
| `document_templates/` | Generated `.docx` uploads (gitignored). Filenames: `HB_MP_Automation_<Category>.docx`. HB library titles: `HB_MP_Automation - …`. |
