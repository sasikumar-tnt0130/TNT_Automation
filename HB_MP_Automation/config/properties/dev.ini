# Property catalog for dev (skeleton — fill URLs and property keys).
# Loaded by load_config from config/properties/*.ini (before secrets.ini).

[dev]
hb_base_url =
mp_base_url =
properties =
legacy_property =
two_step_property =

# Example property section (uncomment and fill):
# [dev.example_property]
# lease_configuration_property_name =
# fms_property_name =
# hb_property_name =
# apw_advance_reservation_enable = false
# mp_state =
# mp_city =
# landing_page_layout =
# value_tier_layout =
# advance_reservation_days = 4
# notes =
